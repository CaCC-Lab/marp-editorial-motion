# カスタマイズガイド

配布した zip（または clone）の中身を、自分好みに変える方法です。触るのは基本 **2 ファイルだけ**:

- **`theme.css`** … 見た目（色・文字サイズ・フォント）
- **`build.mjs`** … モーション（GSAP の動き方）

どちらを変えても、**`node build.mjs` で再ビルドすれば全スライドに一斉反映**されます。原稿（`slides.md`）は触りません。

> AI エージェント（Codex / Claude Code）に開いていれば、下記の「どこを・どう」を **そのまま言葉で頼む** だけでも変えられます（例：「`theme.css` の `--accent` を緑系にして再ビルドして」）。手で開きたい場合の場所も併記します。

---

## 1. 色を変える（`theme.css` の `:root`）

`theme.css` 冒頭の `:root` ブロックに、色は **6 つのトークン**でまとまっています。ここを書き換えるだけで、関連する全パーツが追従します。

```css
:root {
  --ink:         #0e0e10;  /* 本文・見出しの文字色（基本の黒） */
  --paper:       #fafaf7;  /* スライドの背景（紙の色） */
  --rule:        #d8d4c7;  /* ヘアライン罫（上下の細線・表の行区切り） */
  --mute:        #5a574e;  /* hdr/ftr・小さな注記の淡い文字色 */
  --accent:      #1B365D;  /* 藍：小見出し(h3)・引用の左線・コード・リストの — ・pre の左線・時間割の時刻 */
  --accent-soft: #e7ecf2;  /* コード（インライン）の背景 */
  --strike:      #A02C2C;  /* 朱：**強調**（太字）・メッセージの強調語 */
}
```

**どこを変えるとどう変わるか（早見表）**

| 変えたい印象 | 触るトークン | ヒント |
|---|---|---|
| 全体のブランドカラー | `--accent` | 小見出し・罫線・コード・リスト記号が一斉に変わる（いちばん効く） |
| 強調（太字）の色 | `--strike` | `**…**` と メッセージの強調語の色 |
| 背景の地色 | `--paper` | 例：オフホワイト→淡いグレー。`--ink` とのコントラストは保つ |
| 文字色 | `--ink` | 背景を暗くしたら、ここを明るく（ダーク化は下の注意参照） |
| コードの下地 | `--accent-soft` | `--accent` に合わせて薄い同系色にすると馴染む |
| 罫線の濃さ | `--rule` | 上下の細線・表の区切りの色 |

> **ダークテーマにするには**：`--paper` を暗色、`--ink` を明色にし、`--accent` / `--accent-soft` / `--rule` / `--mute` もコントラストが出るよう調整します。`pre`（コードブロック）は別途 `background: var(--ink)` を使っているので、暗背景にする場合は `pre` の `background` も見直してください（`theme.css` 内 `pre { … }`）。

---

## 2. 文字サイズを変える（`theme.css` の `--fs-*`）

サイズも `:root` に **役割ごとのトークン**で揃っています。「文字がスライドからはみ出す／小さすぎる」時は、ここを数 pt 動かします。

| トークン | 既定 | 効く場所 |
|---|---|---|
| `--fs-body` | 26pt | 本文（いちばん使う。**はみ出す時はまずここを 1〜2pt 下げる**） |
| `--fs-lead` | 22pt | リード文（`.lead`）・引用 |
| `--fs-h1` | 52pt | 大見出し（`#`） |
| `--fs-h2` | 34pt | スライド見出し（`##`） |
| `--fs-h3` | 22pt | 小見出し（`###`） |
| `--fs-credo` | 56pt | メッセージスライドの主文（`.credo`） |
| `--fs-table` | 17pt | 表の本文 |
| `--fs-code` / `--fs-pre` | 21 / 19pt | インラインコード / コードブロック |
| `--fs-hdr` / `--fs-ftr` | 11 / 10pt | ヘッダ・フッタ |

（他にも表紙・時間割・付録用の `--fs-title-*` `--fs-flow-*` などがあります。すべて `:root` 内。）

---

## 3. フォントを変える

2 か所をセットで差し替えます（`theme.css`）:

1. 冒頭の `@import url('https://fonts.googleapis.com/css2?family=...')` … 読み込むフォント
2. `section { font-family: … }`（本文）と `h1, h2 { font-family: … }`（見出し）… 適用先

既定は本文 = Noto Sans JP、見出し = Noto Serif JP、コード/番号 = JetBrains Mono です。

---

## 4. アニメーション（GSAP）を変える（`build.mjs`）

動きは `build.mjs` の中の **`INJECTION`** に 1 か所だけ定義されています。各スライドが表示された瞬間、要素が下から順にフェードインします。

```js
gsap.from(targets, {
  opacity: 0,        // 透明から出現（フェードイン）
  y: 28,             // 28px 下から上へ
  duration: 0.7,     // 1 要素あたりの時間（秒）
  stagger: 0.09,     // 要素ごとにずらす間隔（秒）＝順次に出る
  ease: 'power3.out' // 緩急（最初速く→最後ゆっくり）
});
```

対象（`targets`）は見出し・リード・引用・リスト項目・表・コードなど（同ファイルの `querySelectorAll(...)` を参照）。

**どこを変えるとどう変わるか**

| やりたいこと | 変える値 | 例 |
|---|---|---|
| 動きを速く／ゆっくり | `duration` | `0.4`（キビキビ）/ `1.0`（ゆったり） |
| 順次の畳み掛け／間延び | `stagger` | `0.04`（畳み掛け）/ `0.18`（ゆったり） |
| 動きの幅を控えめ／大きく | `y` | `12`（控えめ）/ `48`（大きく） |
| 横から滑り込ませる | `x:` を追加 | `x: 40,` を足す |
| ズームで出す | `scale:` を追加 | `scale: 0.9,` を足す |
| 弾むように出す | `ease` | `'back.out(1.7)'`、他に `'power2.out'` `'expo.out'` |
| アニメを止める | `INJECTION` ごと削除、または `duration: 0` | 静的な HTML になる |

変えたら **再ビルド**（`node build.mjs your.md`）。PDF はアニメ非対象なので影響しません。

> **GSAP CDN が読めない時**：`build.mjs` の `https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js` を `https://unpkg.com/gsap@3.14.2/dist/gsap.min.js` に差し替えると、別 CDN から読めます。

---

## 5. AI エージェントに頼む例

このリポを Codex / Claude Code で開いていれば、上記を言葉で頼めます:

```
theme.css の :root の --accent を #2E7D32（緑）にして、再ビルドして。
```
```
build.mjs の gsap.from の duration を 0.4、stagger を 0.05 にして、再ビルドして。
```
```
本文が一部はみ出しているので、theme.css の --fs-body を 1pt 下げて、再ビルドして。
```

変更前に **diff（差分）が提示**されるので、想定外なら承認前に却下できます。気に入った `theme.css` は、次回そのまま使い回せます（中身＝`slides.md` だけ AI に作らせれば完成）。
